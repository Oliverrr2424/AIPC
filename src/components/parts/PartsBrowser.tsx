"use client";import { useMemo,useState } from "react";import Link from "next/link";import { ArrowRight,MagnifyingGlass } from "@phosphor-icons/react";import type { Part,PartCategory } from "@/types/parts";import { formatPrice } from "@/lib/pricing/priceEstimator";
const categories:["all"|PartCategory,string][]=[["all","All parts"],["cpu","CPUs"],["gpu","GPUs"],["motherboard","Motherboards"],["ram","Memory"],["storage","Storage"],["cooler","Cooling"],["psu","Power"],["case","Cases"]];
export function PartsBrowser({parts}:{parts:Part[]}){const [category,setCategory]=useState<"all"|PartCategory>("all"),[query,setQuery]=useState("");const shown=useMemo(()=>parts.filter(p=>(category==="all"||p.category===category)&&(`${p.name} ${p.brand} ${p.tags.join(" ")}`).toLowerCase().includes(query.toLowerCase())),[parts,category,query]);return <><div className="mt-9 flex flex-col gap-4 lg:flex-row lg:items-center lg:justify-between"><div className="flex gap-2 overflow-x-auto pb-2">{categories.map(c=><button key={c[0]} onClick={()=>setCategory(c[0])} className={`whitespace-nowrap rounded-lg px-3 py-2 text-sm font-medium ${category===c[0]?"bg-[var(--accent)] text-white":"bg-[var(--panel)] text-[var(--muted)] hover:text-[var(--text)]"}`}>{c[1]}</button>)}</div><label className="relative block lg:w-72"><MagnifyingGlass className="absolute left-3 top-3 text-[var(--muted)]"/><input value={query} onChange={e=>setQuery(e.target.value)} className="h-11 w-full rounded-lg border border-[var(--line)] bg-[var(--panel)] pl-10 pr-3 text-sm" placeholder="Search parts"/></label></div><div className="mt-6 grid gap-3 md:grid-cols-2 xl:grid-cols-3">{shown.map(p=><Link key={p.id} href={`/parts/${p.id}`} className="group surface flex min-h-48 flex-col rounded-xl p-5 hover:border-[var(--accent)]"><div className="flex items-start justify-between"><span className="mono text-[10px] uppercase text-[var(--muted)]">{p.category}</span><ArrowRight className="text-[var(--muted)] group-hover:text-[var(--accent)]"/></div><h2 className="mt-8 text-lg font-semibold">{p.name}</h2><p className="mt-2 line-clamp-2 text-sm leading-6 text-[var(--muted)]">{p.summary}</p><div className="mono mt-auto pt-5 text-sm font-semibold">{formatPrice(p.price,"USD")}</div></Link>)}</div>{!shown.length&&<div className="mt-10 rounded-xl border border-dashed border-[var(--line)] p-12 text-center text-[var(--muted)]">No parts match this filter.</div>}</>}
