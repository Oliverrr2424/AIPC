"use client";import { ResponsiveContainer,RadarChart,PolarGrid,PolarAngleAxis,Radar } from "recharts";import type { BuildRecommendation } from "@/types/build";
export function PerformancePanel({build}: {build:BuildRecommendation}){const p=build.parts;const data=[{subject:"Gaming",value:(p.gpu.gamingScore1440p*.78+p.cpu.gamingScore*.22)},{subject:"AI",value:p.gpu.aiScore},{subject:"CPU",value:p.cpu.productivityScore},{subject:"Memory",value:Math.min(100,p.ram.capacityGb/1.28)},{subject:"Storage",value:Math.min(100,(p.storage.readSpeedMb||500)/75)}];const perf=build.performance;return <section className="surface rounded-2xl p-6"><div className="flex items-start justify-between"><div><h2 className="text-lg font-semibold">Performance profile</h2><p className="mt-1 text-sm text-[var(--muted)]">Internal relative scoring</p></div><span className="mono text-xs text-[var(--muted)]">0-100</span></div><div className="mt-3 h-64"><ResponsiveContainer width="100%" height="100%"><RadarChart data={data} outerRadius="70%"><PolarGrid stroke="var(--line)"/><PolarAngleAxis dataKey="subject" tick={{fill:"var(--muted)",fontSize:11}}/><Radar dataKey="value" stroke="var(--accent)" fill="var(--accent)" fillOpacity={.18} strokeWidth={2}/></RadarChart></ResponsiveContainer></div><div className="grid grid-cols-2 gap-3">{perf.gaming&&<div className="rounded-xl bg-[var(--panel-2)] p-4"><div className="text-xs text-[var(--muted)]">{perf.gaming.resolution} gaming</div><strong className="mt-1 block">{perf.gaming.estimatedFpsTier}</strong></div>}{perf.ai&&<div className="rounded-xl bg-[var(--panel-2)] p-4"><div className="text-xs text-[var(--muted)]">Local AI</div><strong className="mt-1 block">{perf.ai.localLlmTier}</strong></div>}</div></section>}
